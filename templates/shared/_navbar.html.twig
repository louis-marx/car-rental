<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">Car Rental</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="/">Acceuil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('parking_list') }}">Nos parkings</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('voiture_list') }}">Nos véhicules</a>
				</li>
			</ul>
			<ul class="navbar-nav">
				{% if app.user %}
					<li class="nav-item">
						<a href="{{ path('reservation_list') }}" class="nav-link">
							Mes réservations
						</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('security_logout')}}" class="btn btn-danger">Logout</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="#" class="nav-link">Inscription</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('security_login') }}" class="btn btn-success">Login</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
